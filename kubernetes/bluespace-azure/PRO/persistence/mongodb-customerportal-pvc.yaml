apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-customerportal-pvc
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: manual
  resources:
    requests:
      storage: 128Gi
  selector:
    matchLabels:
      purpose: mongodb-customerportal